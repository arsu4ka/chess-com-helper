const root = document.getElementById("root");

if (root) {
	root.innerHTML = `
		<div class="w-80 p-6">
			<h1 class="text-2xl">Options</h1>
			<p class="text-base">Extension settings page placeholder.</p>
		</div>
	`;
}
